#include <iostream>
#include <limits>
#include <cstdlib>

void FizzBuzz(int number) {

    if(!(number / 3 >= 1 && number / 5 >= 1))
        std::cout << number;
    else if(number / 3 >= 1)
        puts("Fizz");
    else if(number )

        

}

int main() {
    FizzBuzz(3);
    FizzBuzz(2);
}
